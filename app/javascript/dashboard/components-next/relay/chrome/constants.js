/** Filter / list-filter control text — 13px / 400. `!` wins over Button size classes. */
export const RELAY_FILTER_CLASS = '!text-[13px] !font-normal';

/** Table column header — 14px / 600, muted. Title Case in copy, never uppercase. */
export const RELAY_TABLE_HEAD_CLASS =
  'text-[14px] font-semibold capitalize text-muted-foreground';
